import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{supabase}from'./lib/supabase';import'./style.css';
const tools=[['🔥','热门 AI','ChatGPT','AI通用助手'],['✍️','AI写作','DeepSeek','写作、分析与问答'],['🎨','AI绘图','Gemini','图片与创意生成'],['🎬','AI视频','AI视频工坊','短视频创作'],['🎵','AI音乐','AI音乐助手','音乐创作'],['🗣️','AI配音','AI配音助手','文字转语音'],['🤖','AI助手','Claude','智能对话'],['💻','AI编程','AI编程助手','代码生成与分析'],['📱','自媒体工具','AI爆款工作台','选题、文案、分镜']];
function App(){const[sess,setSess]=useState(null),[balance,setBalance]=useState(null),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[mode,setMode]=useState('login'),[msg,setMsg]=useState('');useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(({data})=>setSess(data.session));const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSess(s));return()=>subscription.unsubscribe()},[]);useEffect(()=>{if(sess&&supabase)supabase.from('token_balances').select('balance').eq('user_id',sess.user.id).single().then(({data})=>setBalance(data?.balance??0));else setBalance(null)},[sess]);async function auth(){setMsg('');if(!supabase){setMsg('请先在 Vercel 配置 Supabase 环境变量');return}let r=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});if(r.error)setMsg(r.error.message);else setMsg(mode==='login'?'登录成功':'注册成功，请检查邮箱确认（如项目开启了邮箱确认）')}return <div className="app"><header><div className="logo">🛒 AI工具超市</div><div className="account">{sess?<><span>{sess.user.email}</span><b>🪙 {balance??'…'} Token</b><button onClick={()=>supabase.auth.signOut()}>退出</button></>:<button onClick={()=>document.getElementById('auth').scrollIntoView()}>登录/注册</button>}</div></header><main><section className="hero"><h1>AI工具，一站式超市</h1><p>发现、使用和管理你的 AI 工具与模型</p><div className="search">🔎 搜索 AI 工具……</div></section>{!sess&&<section id="auth" className="auth"><h2>{mode==='login'?'登录 AI工具超市':'注册 AI工具超市'}</h2><input placeholder="邮箱" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="密码" value={password} onChange={e=>setPassword(e.target.value)}/><button className="primary" onClick={auth}>{mode==='login'?'登录':'注册'}</button><p>{msg}</p><button className="link" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'没有账号？立即注册':'已有账号？返回登录'}</button></section>}<section><h2>🧰 AI工具商城</h2><div className="grid">{tools.map(t=><article className="card" key={t[2]}><div className="icon">{t[0]}</div><small>{t[1]}</small><h3>{t[2]}</h3><p>{t[3]}</p><button>立即使用</button></article>)}</div></section><section className="panels"><div><h2>⚡ Token</h2><p>当前余额：<strong>{balance??(sess?'读取中':'登录后查看')}</strong></p><button>Token充值</button><button>消费记录</button></div><div><h2>👨‍💻 开发者中心</h2><p>开发者可以提交自己的 AI 模型、API 或 Agent，审核通过后接入平台。</p><button>进入开发者中心</button></div><div><h2>🏢 企业中心</h2><p>企业版支持成员账号与 Token 使用统计。</p><button>进入企业中心</button></div></section></main><footer>🛡️ API 密钥由服务器安全管理　·　🛒 AI工具超市</footer></div>};createRoot(document.getElementById('root')).render(<App/>);
