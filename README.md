# 🛒 AI工具超市 v2.1
React + Vite + Supabase。包含邮箱登录/注册、真实 Token 余额读取、AI工具商城、开发者中心、企业中心 UI。

## 环境变量
复制 `.env.example` 为 `.env`，填写 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`。前端只能使用 Publishable/anon key，禁止放 Secret key。
